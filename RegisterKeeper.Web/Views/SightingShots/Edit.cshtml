@model RegisterKeeper.Web.Models.SightingShot

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>SightingShot</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.Converted)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Converted)
            @Html.ValidationMessageFor(model => model.Converted)
        </div>

        @Html.HiddenFor(model => model.Id)

        <div class="editor-label">
            @Html.LabelFor(model => model.ShotNumber)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ShotNumber)
            @Html.ValidationMessageFor(model => model.ShotNumber)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.ScoreText)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ScoreText)
            @Html.ValidationMessageFor(model => model.ScoreText)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.ShootId)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ShootId)
            @Html.ValidationMessageFor(model => model.ShootId)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
