@model IEnumerable<RegisterKeeper.Web.Models.SightingShot>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Converted)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ShotNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ScoreText)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ShootId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Converted)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ShotNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ScoreText)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ShootId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
