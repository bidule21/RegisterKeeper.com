@model DateTime?

<div data-role="fieldcontain">
    @Html.LabelForModel()
    <input type="datetime" id="@Html.IdForModel()" name="@Html.NameForModel()"
        value="@ViewData.TemplateInfo.FormattedModelValue" />
    @Html.ValidationMessageFor(model => model)
</div>